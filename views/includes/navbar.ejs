<nav class="navbar navbar-expand-lg border-bottom sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand brand-lockup" href="/"><i class="fa-regular fa-compass"></i><span>Wanderlust</span></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <form class="search-bar mx-auto" action="/listings" method="GET">
          <input name="q" value="<%= typeof query !== 'undefined' && query ? query : '' %>" placeholder="Search destinations" aria-label="Search destinations">
          <button aria-label="Search"><i class="fa-solid fa-magnifying-glass"></i></button>
        </form>
        <div class="navbar-nav ms-auto align-items-lg-center">
          <a class="nav-link" href="/listings/new">Airbnb your home</a>
          <% if(!currentUser) { %>
            <a class="nav-link" href="/signup">Sign up</a>
            <a class="nav-link nav-pill" href="/login">Log in</a>
          <% } else { %>
            <a class="nav-link" href="/wishlist">Wishlist</a>
            <a class="nav-link" href="/bookings">My trips</a>
            <% if(currentUser.isAdmin) { %>
              <a class="nav-link" href="/admin">Admin</a>
            <% } %>
            <span class="nav-link user-label"><i class="fa-solid fa-circle-user"></i> <%= currentUser.username %></span>
            <a class="nav-link nav-pill" href="/logout">Log out</a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>
